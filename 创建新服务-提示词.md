# 创建新服务 — AI 提示词模板

> 给**非研发人员**使用：把下面的提示词整段复制到 Qoder，替换 `【】` 中的内容即可。AI 会自动按 `AI-DEVELOPMENT-SPEC.md` 中的规范执行，无需你关心技术细节。

---

## 使用方式

1. 整段复制下方「提示词正文」
2. 替换 `【新服务名】` 和 `【业务需求】`
3. 粘贴到 Qoder 回车发送
4. 等 AI 完成后查看交付清单

---

## 提示词正文

```text
请先完整读取 sandbox-service-template 仓库中的 AI-DEVELOPMENT-SPEC.md，
然后严格按照该文档的规范执行以下任务：

1. 确保使用最新版的模板代码（如本地已有模板就强制更新到远程最新版，没有就从远程拉取一份）
2. 基于最新模板创建新服务，服务名称为：【新服务名】
3. 实现以下业务需求（如只想创建空工程，删除本行及下方需求即可）：

【业务需求】
```

---

## 占位符填写说明

| 占位符 | 必填 | 规则 | 示例 |
|--------|-----|------|------|
| `【新服务名】` | 是 | 小写字母开头，仅 `a-z` / `0-9` / `-` | `order-service`、`my-todo` |
| `【业务需求】` | 仅业务开发场景 | 用大白话说清要做什么 | 见下方示例 |

---

## 业务需求示例

### ✅ 推荐写法（AI 能直接开干）

```text
做一个简单的资产管理功能：
1. 能添加资产（名称、类型、价值、所属人）
2. 能查看列表，支持按类型筛选、按名称搜索
3. 能修改资产信息
4. 能删除资产（需要二次确认）
5. 列表分页，每页 20 条
```

### ❌ 不够清晰（AI 会反复确认）

```text
做个管理功能。
```

---

## 完整使用示例

```text
请先完整读取 sandbox-service-template 仓库中的 AI-DEVELOPMENT-SPEC.md，
然后严格按照该文档的规范执行以下任务：

1. 确保使用最新版的模板代码（如本地已有模板就强制更新到远程最新版，没有就从远程拉取一份）
2. 基于最新模板创建新服务，服务名称为：order-service
3. 实现以下业务需求：

做一个订单管理功能：
1. 能创建订单，字段：订单号、客户名、商品列表、金额、状态
2. 能查看订单列表，支持按状态筛选、按订单号搜索
3. 能修改订单状态（待处理 → 处理中 → 已完成 / 已取消）
4. 不允许直接删除订单，只能取消
```

---

## 常见问题

**Q：模板代码和远程不一致怎么办？**  
不用管，AI 会自动用远程版本覆盖本地。

**Q：服务名字写错了怎么办？**  
让 AI 删除新建的工程目录后重新发提示词即可。

**Q：Forgejo 上还没有空仓库怎么办？**  
先到 [`https://gitea.meos.plus`](https://gitea.meos.plus) 手动创建（参考 Spec **Part 2 步骤一**），再发提示词。

**Q：发完提示词 AI 中途要我确认怎么办？**  
回复"按 spec 文档执行"即可。

**Q：完成后新工程在哪里？**  
默认在 `~/persagy/coding/【新服务名】/` 目录下。

---

> 详细规范、目录结构、技术栈说明请查看 [`AI-DEVELOPMENT-SPEC.md`](./AI-DEVELOPMENT-SPEC.md)

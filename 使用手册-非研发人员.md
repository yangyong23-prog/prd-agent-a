# 使用手册 — 非研发人员

> 你只需要会**发提示词**。

---

## 准备工作

用 **Qoder** 打开你电脑上任意一个文件夹（代码会自动放到提示词里指定的工作目录），即可开始。

> 不需要下载任何文件，AI 会在线读取规范文档。

---

## 开发业务需求

打开 Qoder，把下面这段提示词**复制粘贴**进去，填写三处 `【】` 内容后发送：

```
本任务将在 sandbox 环境中执行。
请先读取以下规范文档：
https://gitea.meos.center/sandbox/sandbox-service-template/raw/branch/main/AI-DEVELOPMENT-SPEC.md

然后基于模板创建新服务：

工作目录：【新服务代码存放路径】
服务名称：【英文，如 order-service】
业务需求：
【大白话描述你要做什么，不填则自动创建最小可运行骨架】

完成后自动提交代码并推送，输出访问地址。
```

> **不填业务需求也可以！** AI 会自动创建一个包含登录、首页、常用布局的最小可运行骨架，部署后直接可访问。

**工作目录怎么填：**

| 系统 | 填写示例 |
|------|---------|
| Mac | `/Users/你的用户名/sandbox-workspace` |
| Windows | `C:\Users\你的用户名\sandbox-workspace` |

> 填你想把代码放在哪个文件夹，不存在会自动创建。

---

AI 会自动完成所有步骤，你**什么都不用做**：

| 步骤 | 谁来做 |
|------|--------|
| 在 Forgejo 创建仓库 | AI 自动 |
| 克隆代码到工作目录 | AI 自动 |
| 开发业务需求 | AI 自动 |
| 提交并推送代码 | AI 自动 |
| 构建镜像 + 部署到 K8s | CI/ArgoCD 自动 |
| **访问服务** | **你** |

约 3–5 分钟后，打开以下地址即可访问：

```
https://sandbox.meos.center/【服务名】-web/
```

---

## 更新服务（收到通知后执行）

当研发同事通知你「模板服务有新能力更新，请同步到你的工程」时，用 Qoder 打开你的服务工程目录，发以下提示词：

```
本任务将在 sandbox 环境中执行。
请先读取以下规范文档：
https://gitea.meos.center/sandbox/sandbox-service-template/raw/branch/main/AI-DEVELOPMENT-SPEC.md

然后将模板工程最新修改同步到当前服务工程。

当前服务工程目录：【服务工程完整路径】

要求：
1. 保留我已开发的业务功能代码（apps 目录下的业务路由、页面组件、数据库 schema 等）。
2. 只同步模板工程的基础能力内容（.gitea/、k8s/、packages/、rules/、templates/ 等目录，以及 .qoder/ 配置）。
3. 完成后自动提交并推送，输出改动范围和访问地址。
```

| 占位符 | 怎么填 | 示例 |
|--------|--------|------|
| `【服务工程完整路径】` | 你的服务代码所在目录 | Mac: `/Users/用户名/sandbox-workspace/order-service`　Windows: `C:\Users\用户名\sandbox-workspace\order-service` |

> AI 会保留你已开发的业务功能，只更新基础能力部分。可以放心操作。

---


## 可用的后台接口能力

写业务需求时，直接说「调用 XX 模块拿 XX 数据」，AI 会自动查接口、处理调用，不需要你懂技术细节。

---

### 一、数据服务（63 个接口）

> 提供设备实时运行数据、历史趋势、能耗统计等各类 IoT 数据能力。

| 数据模块 | 能做什么 | 使用场景举例 |
|---------|---------|-------------|
| **设备实时数据** | 查询设备当前最新测量值、动态信息点数据 | 显示某台空调的当前温度、某台水泵的运行状态 |
| **设备历史数据** | 按时间段查询持久化历史测量值、统计某段时间的最大/最小/均值 | 展示过去一周的能耗趋势折线图 |
| **能耗聚合统计** | 按项目/时间/信息点维度汇总能耗数据、电价计算 | 各楼层本月用电量排行、项目电价展示 |
| **设备状态统计** | 统计通风、照明、空调末端、冷机、泵组、安防、排油烟等设备的运行状态数量 | 首页设备健康看板（运行中/停机/故障各多少台） |
| **点位在线状态** | 批量查询设备信息点是否在线、统计在离线比例 | 数据质量监控页面 |
| **人工采集数据** | 录入和查询人工观测数据（如人工抄表数值） | 人工录入水电表读数并展示 |
| **信息点订阅** | 订阅指定设备信息点的实时推送 | 关键设备数据变化时自动刷新页面 |

---

### 二、风险数据服务（134 个接口）

> 提供设备风险点、风险事件、异常事件、资产问题等风险相关数据能力。

| 数据模块 | 能做什么 | 使用场景举例 |
|---------|---------|-------------|
| **风险点** | 分页查询项目下的风险点列表、查看风险等级和详情 | 展示当前项目所有设备的风险分级列表 |
| **风险事件** | 查询风险事件列表、按时间统计发生次数 | 本月风险事件趋势图、各类型风险数量统计 |
| **异常事件** | 查询实时异常事件（热数据）和历史异常事件（冷数据）、按天统计数量 | 今日未处理异常事件列表、异常事件历史汇总报表 |
| **资产问题** | 查询异常资产列表、统计问题发生次数按时间分布、查看问题原因分析 | 问题最多的设备 TOP10、某设备历史故障记录 |
| **执行计划** | 查询巡检/维保计划详情和列表 | 本周巡检计划进度总览 |
| **巡检/维保策略** | 查询策略事项列表和详情、已发布策略清单 | 查看某类设备的巡检策略内容 |
| **IOT 报警规则** | 查询设备绑定的报警规则详情 | 查看某台设备的超温报警阈值配置 |

---

### 三、工程事务服务（27 个接口）

> 提供工单的创建、查询、费用统计等工程事务能力。

| 数据模块 | 能做什么 | 使用场景举例 |
|---------|---------|-------------|
| **工单查询** | 按工单ID、对象实例、异常事件、时间范围查询工单列表及详情 | 查看某台设备关联的所有维修工单 |
| **工单任务** | 查询计划巡检/计划维保任务、查询任务执行结果和观测记录 | 展示本月巡检完成情况统计 |
| **工单流程** | 查询工单流程状态、工作类型列表 | 查看某工单当前处理进度 |
| **工单费用** | 查询已完成工单的费用明细 | 本季度维修费用汇总报表 |
| **批量创建工单** | 根据对象实例批量创建工单任务 | 触发风险事件后自动批量派发工单 |
| **反馈记录** | 查询工单处理过程中的上报信息和反馈记录 | 查看工单处理人的现场反馈内容 |

---

### 四、准静态物理世界（156 个接口）

> 提供建筑空间结构、设备实例、设备位置关系、三维几何数据等物理世界数据能力。

| 数据模块 | 能做什么 | 使用场景举例 |
|---------|---------|-------------|
| **空间结构查询** | 查询项目下的建筑列表、楼层列表、功能区列表、各类型空间列表 | 展示楼层选择下拉、空间树导航 |
| **设备实例查询** | 按专业/楼层/空间/类型查询设备列表，支持条件过滤 | 查询某楼层所有空调设备、按类型筛选设备 |
| **设备位置** | 查询设备所在的空间和楼层、查询附近范围内的同类设备 | 显示设备所在楼层位置、周边同类设备列表 |
| **关系与路由** | 查询设备间是否有关联关系、查询从一个设备到另一个设备的路径 | 查看某台水泵服务哪些空间、管道路径追踪 |
| **几何数据** | 查询设备/空间的坐标中心点、外包围盒、轮廓线、骨架线 | 在平面图上定位设备坐标 |
| **三维模型** | 获取楼层/空间的三维场景文件、设备模型文件 | 三维可视化展示楼层设备分布 |
| **资产台账** | 查询资产实例列表、信息点统计、多关系查询 | 资产清单报表、资产归属关系展示 |

---

## 业务需求怎么写

拆成几条，每条说清「能做什么」「需要哪些字段」「有什么特殊要求」：

```
做一个简单的资产管理功能：
1. 能添加资产（名称、类型、价值、所属人）
2. 能查看列表，支持按类型筛选、按名称搜索
3. 能修改资产信息
4. 能删除资产（需要二次确认）
5. 列表分页，每页 20 条
```

如果需要用到后台数据，直接在需求里说明：

```
做一个风险看板：
1. 展示当前项目下所有设备的风险等级分布（调用风险数据服务）
2. 点击风险设备，展示对应的工单列表（调用工程事务服务）
```

---

## 出问题怎么办

| 情况 | 处理方式 |
|------|---------|
| AI 中途要我确认 | 回复「按规范文档执行」 |
| AI 报错但能继续 | 把错误信息复制给 AI，让它自己修 |
| AI 反复失败 | 停下来，找研发同事 |

---

## 安全红线

不要把**数据库密码、Token、客户真实姓名/手机号/身份证号**写进提示词。

# 强制更新工程到最新代码 — AI 提示词模板

> 给**非研发人员**使用：当**模板工程 `sandbox-service-template`** 落后于远程仓库时，把下面的提示词复制到 Qoder，AI 会自动拉取最新代码并强制覆盖本地版本。

> ⚠️ **仅适用于模板工程**：本提示词只用于更新 **模板仓库 `sandbox-service-template`** 自身。**严禁**用于基于模板创建出来的业务服务工程（例如 `dfz-sandbox` 等派生服务）——业务工程含有你和团队的真实改动，强制覆盖会**永久丢失未推送的本地代码**。基于模板创建的业务服务请走团队内常规的 `git pull` / 分支合并流程。

---

## 适用场景

仅限以下一种情况：

- 你打开的目录是 **`sandbox-service-template` 模板工程本身**，且平台维护者更新了模板，需要拿到最新版

不适用场景（请勿使用本提示词）：

- ❌ 基于模板创建的业务服务工程落后远程 → 走常规 git 流程
- ❌ 想保留本地任何未提交修改 → 强制覆盖会销毁这些修改

> ℹ️ `.env` 文件不会被影响（在 `.gitignore` 中），可放心更新。

---

## 使用方式

1. 用 Qoder 打开 **`sandbox-service-template` 模板工程目录**（再次确认不是业务服务工程）
2. 复制下方「提示词正文」
3. 粘贴到 Qoder 回车发送

---

## 提示词正文

### 一句话版（推荐）

```text
当前工程是模板仓库 sandbox-service-template，请拉取远程最新代码并强制覆盖本地。
```

### 详细版（依赖或数据库可能变更时使用）

```text
当前工程是模板仓库 sandbox-service-template，请拉取远程最新代码并强制覆盖本地版本，并按需执行：

- 如 package.json 或 pnpm-lock.yaml 有变化，重新执行 pnpm install
- 如 apps/server/prisma/schema.prisma 有变化，重新生成 Prisma 客户端

完成后输出：远程新增了哪些提交、依赖是否变更、是否需要重启服务。

注意：如果当前目录不是 sandbox-service-template，请直接拒绝执行强制覆盖。
```

---

## 完整使用示例

打开 **模板工程 `sandbox-service-template`** 后，粘贴：

```text
当前工程是模板仓库 sandbox-service-template，请拉取远程最新代码并强制覆盖本地，完成后告诉我有哪些更新。
```

AI 会输出类似：

```text
✅ 已同步到远程最新（origin/main: a1b2c3d）
📥 拉取了 5 个新提交：
   - feat(server): 新增导出接口
   - fix(web): 修复登录页跳转
   - ...
📦 检测到 package.json 变化，已重新执行 pnpm install
🔄 建议：重启 pnpm dev 让最新代码生效
```

---

## 常见问题

**Q：`.env` 文件会丢吗？**  
不会。`.env` 在 `.gitignore` 中，git 操作不会影响它。

**Q：远程主分支不是 `main` 怎么办？**  
在提示词后追加："远程主分支是 `develop`"（或对应分支名）。

**Q：执行后服务起不来怎么办？**  
继续回复："服务起不来，请检查端口占用、依赖完整性、Prisma client 是否需要重新生成"。

**Q：只想看远程有什么更新但不实际操作怎么办？**  
把提示词改成："只查询远程最新有什么变化，不要修改本地代码"。

**Q：基于模板派生的业务服务工程也想拉远程最新代码怎么办？**  
**不要使用本提示词**。业务服务工程的更新请走常规 git 流程（`git pull`、必要时人工解决冲突），避免误删本地未推送的业务改动。

---

## 与「创建新服务」提示词的区别

| 场景 | 用哪份提示词 |
|------|-------------|
| 想从 0 创建一个新服务工程 | [`创建新服务-提示词.md`](./创建新服务-提示词.md) |
| **模板工程** `sandbox-service-template` 落后了，想拉最新代码 | **本文档** |
| 基于模板派生的**业务服务工程**落后了 | 走常规 `git pull` 流程，**不**使用本文档 |

---

> 详细规范请查看 [`AI-DEVELOPMENT-SPEC.md`](./AI-DEVELOPMENT-SPEC.md) 的 **Part 1：模板代码更新操作**
